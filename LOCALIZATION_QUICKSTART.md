# 🚀 Быстрый старт: Система локализации QMS API

## ⚡ За 30 секунд

```bash
# 1. Выполнить миграцию
php artisan migrate

# 2. Готово! Система работает
```

---

## 📡 Основные API вызовы

### Получить список языков и статусы
```bash
curl -X GET http://localhost:8000/api/v1/languages \
  -H "Authorization: Bearer TOKEN"
```

**Ответ:**
```json
{
  "supported_locales": ["en", "ru", "de"],
  "current_locale": "ru",
  "language_names": {
    "en": "Английский",
    "ru": "Русский",
    "de": "Немецкий"
  },
  "status_names": {
    "online": "Онлайн",
    "chatty": "Готов поболтать",
    "angry": "Злой"
  }
}
```

### Сменить язык пользователя
```bash
curl -X PUT http://localhost:8000/api/v1/users/locale \
  -H "Authorization: Bearer TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"locale": "de"}'
```

---

## 💻 Как это работает в коде

### В контроллере
```php
$statusName = __('statuses.online'); // "Онлайн" (если locale=ru)
```

### В Service
```php
$service = new LocalizationService();
$names = $service->getStatusNames(); // Все локализованные статусы
```

---

## 🗂️ Структура файлов

```
resources/lang/
├── en/
│   ├── languages.php    (название языков на англ.)
│   └── statuses.php     (статусы на англ.)
├── ru/
│   ├── languages.php
│   └── statuses.php
└── de/
    ├── languages.php
    └── statuses.php
```

---

## 🔤 Поддерживаемые языки

| Код | Название | Примечание |
|-----|----------|-----------|
| en  | English  | По умолчанию |
| ru  | Русский  | Полная поддержка |
| de  | Deutsch  | Полная поддержка |

---

## ➕ Добавить новый язык (например, испанский)

1. Создать файлы:
   ```bash
   touch resources/lang/es/languages.php
   touch resources/lang/es/statuses.php
   ```

2. Обновить 3 константы с 'es':
   - `SetLocale.php`
   - `LocalizationService.php`
   - `UpdateUserLocaleRequest.php`

3. Заполнить переводы в файлах

---

## ✅ Автоматические проверки

Система автоматически:
- ✅ Определяет язык из заголовка Accept-Language
- ✅ Использует язык пользователя из профиля
- ✅ Валидирует выбранный язык
- ✅ Локализует все текстовые данные

---

## 📚 Полная документация

- `LOCALIZATION.md` - Полное описание системы
- `LOCALIZATION_SETUP.md` - Инструкция по установке
- `LOCALIZATION_REPORT.md` - Детальный отчет

---

**Готово к использованию! 🎉**

